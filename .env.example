# Copy this file to `.env` (which is gitignored) for LOCAL runs only.
# NEVER commit real values. In GitHub Actions these come from repo secrets.

# Travelpayouts (free: travelpayouts.com -> Dashboard -> Developers -> API token)
TRAVELPAYOUTS_TOKEN=your-travelpayouts-api-token
# Optional affiliate marker appended to Aviasales booking links
# TRAVELPAYOUTS_MARKER=your-marker

# Email (only needed if you send reports locally; Actions uses secrets)
# MAIL_USERNAME=you@gmail.com
# MAIL_PASSWORD=your-gmail-app-password
# MAIL_TO=you@gmail.com
